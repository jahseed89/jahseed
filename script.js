let myName = "Jahseed";
let courses = "HTML, Css, JavaScript";

console.log("My Name is: " + myName);
console.log("The courses am taking are: " +courses);

for(var counter = 1; counter <= 200; counter++) {
	if(counter % 2 !== 0) {
		console.log(counter)
	}
}